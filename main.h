#ifndef _MAIN_H_
#define _MAIN_H_
#include <stdlib.h>
#include <unistd.h>
#include <stdarg.h>
#include <string.h>

/* PRINTF PROTOTYPE */
int _printf(const char *format, ...);

/* OTHERS */
int _putchar(char c);
int _len(char *str);

#endif
